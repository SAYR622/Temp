<Window x:Class="VMS.UI.SiteForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Icon="/Images/logo2.png"
        Title="Add New Heritage Site"
        Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC">

    <Border Padding="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" 
                       Text="Heritage Site Details" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Margin="0,0,0,25"/>

            <StackPanel Grid.Row="1">

                <TextBox x:Name="txtSiteName" 
                         materialDesign:HintAssist.Hint="Site Name"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         FontSize="14" Margin="0,0,0,20"/>

                <TextBox x:Name="txtProvince" 
                         materialDesign:HintAssist.Hint="Province"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         FontSize="14" Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtLatitude" 
                             Grid.Column="0"
                             materialDesign:HintAssist.Hint="Latitude"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             FontSize="14"/>

                    <TextBox x:Name="txtLongitude" 
                             Grid.Column="2"
                             materialDesign:HintAssist.Hint="Longitude"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                             FontSize="14"/>
                </Grid>

            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">

                <Button x:Name="btnSave" 
                        Content="➕ Add" 
                        Width="120"
                        Background="#22C55E" BorderBrush="#22C55E"
                        Click="btnSave_Click"/>

            </StackPanel>

        </Grid>
    </Border>
</Window>